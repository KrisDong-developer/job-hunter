/**
 * 猎聘城市码表（370 个实测城市码 + 「全国」= 空串），从 `DEFAULT_LIEPIN_CONFIG.cityCodes`
 * 原样抽出：键值 / 注释 / 顺序 / 缩进一律不变（注释是采样证据，跟着数据走）。
 *
 * 采样方式、原始数据与三个坑见 `test/fixtures/liepin-city-codes.json`；完整实测记录见 `./index.ts` 文件头。
 */
export declare const LIEPIN_CITY_CODES: Record<string, string>;
//# sourceMappingURL=codes.d.ts.map