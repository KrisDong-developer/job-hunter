node_modules/
.build/
.test-build/
*.tsbuildinfo

# lib/ 与 client/ 是构建产物，但**故意提交进仓库**：
# 别人 git clone / `dsh plugin add github:…` 下来就能直接安装，不需要本地构建。
# 改了 src/ 之后请务必 `npm run build` 再提交，否则别人装到的还是旧产物。
# 唯一忽略的是 sourcemap —— 运行时用不到，只为省仓库体积。
lib/**/*.map
client/**/*.map
